venv/
node_modules/

__pycache__/
*.pyc
*.pyo
*.pyd

.env
.env.*
!.env.example

purvex.db

# Per-install local state (scripts/purvex.sh) — specific to the machine
# that ran setup, never meant to ship or be shared across installs
.purvex_admin_bootstrapped
.purvex_boot_choice

assets/

.DS_Store
Thumbs.db

.idea/
.vscode/

*.log

# PyInstaller build artifacts
build/
scripts/purvex.exe

# Working/temp files
cookiejar.txt
plan.md
.claude/

# Local Alembic/SQLite reconciliation helpers — must not ship
backend/_fix_*.py

# Stray local logs/output captured by run scripts
tmp-start-*.log

# License signing private key (scripts/issue_license.py) — never commit this
backend/.license_signing_key.pem

# License poller's local log (scripts/run_poller.ps1)
backend/.license_poller.log
