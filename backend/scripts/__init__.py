# Utility scripts for one-off maintenance tasks (e.g. data backfills).
#
# These are not imported by the main application and should be run as
# `python -m scripts.<name>` from the `backend/` directory.


