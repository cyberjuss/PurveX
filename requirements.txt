# NOTE: Versions reflect the 2026-05 security audit. Bumps fix CVEs in
# requests (CVE-2024-35195/47081, CVE-2026-25645), python-dotenv (CVE-2026-28684),
# starlette (CVE-2025-54121, CVE-2025-62727), and replace the unmaintained
# `python-jose` library (CVE-2024-33663/33664) with PyJWT.
fastapi>=0.131,<0.140
uvicorn>=0.30,<1
gunicorn>=23.0.0
SQLAlchemy>=2.0.40,<3
pydantic>=2.11,<3
# 5.0.0 fixes CVE-2026-44405 (SHA-1 host-key allowance) — the prior <5 pin
# was tracking upstream for this exact patch (see 2026-05 audit note above).
paramiko>=5.0.0,<6
python-dotenv>=1.2.2,<2
pydantic-settings>=2.6,<3
requests>=2.32.4,<3
httpx>=0.27.0
PyYAML>=6.0.2
asyncpg>=0.30.0
aiosqlite>=0.21.0
PyJWT[crypto]>=2.10.1,<3
python-multipart>=0.0.18
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0,<5
email-validator
aiosmtplib>=3.0,<4
# Pin starlette explicitly so resolver enforces the post-CVE-2025-62727 floor
# regardless of which FastAPI patch release is selected above.
starlette>=0.49.1
loguru
reportlab>=4.0.0
alembic>=1.13.0
redis>=5.0.0
arq>=0.26.0
python-json-logger>=2.0.7
prometheus-client>=0.20.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp>=1.25.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-sqlalchemy>=0.46b0
opentelemetry-instrumentation-httpx>=0.46b0

